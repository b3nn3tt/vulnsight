[pytest]
# Only collect the project's own tests. Without this, pytest would descend into
# the bundled virtualenv (env/) and try to collect the stdlib's test suites.
testpaths = tests
norecursedirs = env .venv .git .vulnsight __pycache__
